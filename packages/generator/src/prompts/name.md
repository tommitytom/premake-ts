I am going to provide you with a lowercase function name, and I would like you to convert it in to lowerCamelCase. Some examples:

`implibextension` would become `impLibExtension`
`linktimeoptimization` would become `linkTimeOptimization`

DO NOT OVERDO IT! If there is not an obvious way of making the name lowerCamelCase, just return the original string.
Examples of function names that should remain the same: `symbols`, `inlining`, `atl`, `language`.

The string should *ALWAYS* be the same length as the string that is passed in.

Your response should contain only the lowerCamelCase version of the function name I provide, and nothing else.

The function name: